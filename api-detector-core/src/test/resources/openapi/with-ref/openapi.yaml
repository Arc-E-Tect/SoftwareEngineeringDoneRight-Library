openapi: 3.0.3
info:
  title: With-Ref Test API
  version: "1.0"
paths:
  /users:
    $ref: 'paths/users.yaml'
  /users/{id}:
    get:
      operationId: getUser
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: OK
