openapi: 3.2.0
info:
  title: With-Ref OpenAPI 3.2 Test API
  version: "1.0"
paths:
  /items:
    $ref: 'paths/items.yaml'
  /items/{id}:
    get:
      operationId: getItem
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        '200':
          description: OK
