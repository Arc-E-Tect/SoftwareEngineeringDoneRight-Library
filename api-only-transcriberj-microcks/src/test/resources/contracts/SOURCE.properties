# The contracts under this directory: the reference library's published targets,
# built by this repository's API-Only Publisher with x-fragment-path on. Only
# user-account has an AsyncAPI document; the others describe no events.
#
# To rebuild them: export the library at `library.commit`, then run the Publisher
# at `publisher.commit` there:
#   git -C <Code checkout> archive <library.commit> usable-suspects | tar -x -C <dir>
#   node <publisher>/src/cli.js -C <dir>/usable-suspects build
# and copy dist/<target>/openapi.yaml for each target below.
#
# portfolio is left out: it is `publish: false`, a documentation view rather than a
# contract anyone subscribes to.
library.repository=Arc-E-Tect/SoftwareEngineeringDoneRight-Code
library.path=usable-suspects
library.commit=1cbc14e4f82f8a84410168bd3c65bdcdcb6121d4
publisher.commit=d3c4013
targets=auth-server,relationship,social-network,system-admin,user-account
